apiVersion: v1
kind: Service
metadata:
  name: {{ include "paper-submission.fullname" . }}
  labels:
    {{- include "paper-submission.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paper-submission.selectorLabels" . | nindent 4 }}
