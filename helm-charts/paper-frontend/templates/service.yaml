apiVersion: v1
kind: Service
metadata:
  name: {{ include "paper-frontend.fullname" . }}
  labels:
    {{- include "paper-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paper-frontend.selectorLabels" . | nindent 4 }}
