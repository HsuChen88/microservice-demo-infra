apiVersion: v1
kind: Service
metadata:
  name: {{ include "paper-catalog.fullname" . }}
  labels:
    {{- include "paper-catalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paper-catalog.selectorLabels" . | nindent 4 }}
